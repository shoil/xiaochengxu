<view class="nav center">
    <image src="{{isLatest?noLeft:yesLeft}}" class="radio" bind:tap="onLeft"></image>
    <text class="title">{{title}}</text>
    <image src="{{isFirst?noRight:yesRight}}" class="radio" bind:tap="onRight"></image>
</view>
